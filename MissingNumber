#include <stdio.h>

int main()
{
    int n;
    int s2=0;
    scanf("%d",&n);
    int arr[n-1];
    for(int i=1;i<=n-1;i++)
    {
        scanf("%d",&arr[i]);
    }
    int s1=n*(n+1)/2;
    for(int i=0;i<n;i++)
    {
        s2+=arr[i];
    }
    printf("%d",s1-s2);
    
    return 0;
}
